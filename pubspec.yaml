name: stud_budget_calc
description: "Student Budget Calculator App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

#Flutter Section
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/audios/